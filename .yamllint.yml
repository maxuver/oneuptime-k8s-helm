---
extends: default

rules:
  line-length:
    max: 200
    level: warning
  colons: disable
  braces: disable
  document-start: disable
  truthy:
    check-keys: false
  comments-indentation: disable
  indentation:
    spaces: consistent
    indent-sequences: consistent
